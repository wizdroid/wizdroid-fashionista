# Core dependencies for ComfyUI Outfit Selection Node
# No additional dependencies required for the main node

# Optional dependencies for support nodes (Ollama Vision & LLM)
requests>=2.25.0
pillow>=8.0.0
numpy>=1.19.0
torch>=1.9.0
