# Core dependencies for AI and vision features
requests>=2.25.0
pillow>=8.0.0
numpy>=1.19.0

# Lightweight AI models (optional - for Lightweight Prompter and Vision nodes)
transformers>=4.35.0
torch>=1.9.0
accelerate>=0.21.0
torchvision>=0.10.0

# Note: torch and torchvision are assumed to be available in ComfyUI environment
# but may need to be installed separately for the Lightweight nodes
# Vision models (Florence-2, BLIP/BLIP-2, WD14) will be downloaded automatically on first use
# Models range from 180MB (BLIP base) to 2.7GB (BLIP-2) depending on selection

# Face extraction dependencies
ultralytics>=8.0.0
opencv-python>=4.5.0
segment-anything>=1.0.0

